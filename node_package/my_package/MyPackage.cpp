#include "MyPackage.hpp"

#ifndef _MYPACKAGE
#define _MYPACKAGE

MyPackage(){
    /* specify a function handler to a method from within the class
     * subscriber_ = nodeHandle_.subscribe(topic,
     *                                     queue_size,
     *                                     &ClassName::methodName,
     *                                     this);
     */
}

#endif
